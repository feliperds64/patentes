# from estevam_flask import app, db
# from estevam_flask.models import Dados
#
# with app.app_context():
#     db.create_all()
#
# with app.app_context():
#    meus_registros = Dados.query.all()
#    print(meus_registros)
#    for registro in meus_registros:
#        print(registro.cod_processo)
#
# with app.app_context():
#     db.drop_all()
